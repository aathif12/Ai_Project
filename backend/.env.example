# Environment Configuration
# Copy this file to .env and fill in your values

# ===========================================
# OpenAI Configuration (Required)
# ===========================================
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# ===========================================
# Supabase Configuration (Required)
# ===========================================
# Get these from your Supabase project: Settings > API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

# Database connection string for vector operations
# Found in: Settings > Database > Connection string > URI
# Replace [YOUR-PASSWORD] with your database password
SUPABASE_DB_URL=postgresql://postgres:[YOUR-PASSWORD]@db.your-project-id.supabase.co:5432/postgres

# ===========================================
# Storage Configuration
# ===========================================
UPLOAD_DIR=./uploads

# ===========================================
# Chunking Configuration
# ===========================================
# chunk_size: Target size of each text chunk in tokens
# chunk_overlap: Number of overlapping tokens between chunks
CHUNK_SIZE=800
CHUNK_OVERLAP=100

# ===========================================
# Retrieval Configuration
# ===========================================
# Number of relevant chunks to retrieve for each query
TOP_K_RESULTS=5

# ===========================================
# Model Configuration
# ===========================================
# Embedding model: text-embedding-3-small (1536 dim) or text-embedding-3-large (3072 dim)
# LLM model: gpt-4o-mini (fast & cheap) or gpt-4o (better quality)
EMBEDDING_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini

# Embedding dimensions (must match the embedding model)
# text-embedding-3-small: 1536
# text-embedding-3-large: 3072
EMBEDDING_DIMENSIONS=1536
